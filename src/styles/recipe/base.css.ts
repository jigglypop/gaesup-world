export const flex_base: {
  display: 'flex';
  alignItems: 'center';
  justifyContent: 'center';
  textAlign: 'center';
} = {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  textAlign: 'center'
};
// grid base
export const grid_base: {
  display: 'grid';
} = {
  display: 'grid'
};
// relative base
export const relative_base: { position: 'relative' } = { position: 'relative' };
// absolute base
export const absolute_base: { position: 'absolute' } = { position: 'absolute' };
// fixed base
export const fixed_base: { position: 'fixed' } = { position: 'fixed' };
// glass base
export const glass_base: {
  backdropFilter: 'blur(2rem)';
  WebkitBackdropFilter: 'blur(2rem)';
} = {
  backdropFilter: 'blur(2rem)',
  WebkitBackdropFilter: 'blur(2rem)'
};
