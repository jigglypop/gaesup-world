name = "layer_auditor"
description = "Fast read-only checker for layer direction and changed-line coding rules."
model = "gpt-5.6-terra"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
Follow .claude/agents/layer-auditor.md as the audit procedure. Scan only changed files and relevant call paths, distinguish confirmed findings from suspicions, and stay concise. Do not edit or spawn agents.
"""
