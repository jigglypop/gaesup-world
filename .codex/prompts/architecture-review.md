# Architecture Review

Use the architect agent to compare the proposed slice with `architecture.md`, `world-model.md`, `invariants.md` and `migration.md`. Return decisions, evidence, impact, the smallest migration boundary and completion criteria. Do not implement.
