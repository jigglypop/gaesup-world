# Close Epoch

Run the validation required by the active plan, then use reviewer for self-review. Record passing, failing and unexecuted checks. Move the plan from `active` to `completed` only when every completion criterion is met and unresolved failures are documented.
