import { WorldPropsType } from './types';
export declare function WorldProps({ type, text, position, children, interactive, showMinimap }: WorldPropsType): import("react").JSX.Element;
export * from './types';
