import React from 'react';
import { SpeechBalloonProps } from './types';
import './styles.css';
export declare function SpeechBalloon({ text, position, offset, backgroundColor, textColor, fontSize, padding, borderRadius, borderWidth, borderColor, maxWidth, visible, opacity, children, }: SpeechBalloonProps): React.JSX.Element | null;
export default SpeechBalloon;
