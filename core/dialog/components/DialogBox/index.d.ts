import React from 'react';
export type DialogBoxProps = {
    advanceKey?: string;
    closeKey?: string;
};
export declare function DialogBox({ advanceKey, closeKey }: DialogBoxProps): React.JSX.Element | null;
export default DialogBox;
