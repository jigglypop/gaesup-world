/**
 * Collects WebGL/WebGPU renderer stats every 30 frames and writes them
 * to the Zustand performance slice. Must be placed inside <Canvas>.
 */
export declare function PerformanceCollector(): null;
