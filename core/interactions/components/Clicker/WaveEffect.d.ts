import React from 'react';
interface WaveEffectProps {
    active?: boolean;
    color?: string;
    intensity?: number;
}
export declare const WaveEffect: React.MemoExoticComponent<({ active, color, intensity }: WaveEffectProps) => React.JSX.Element>;
export {};
