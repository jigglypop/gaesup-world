export declare const palette: {
    inherit: string;
    current: string;
    transparent: string;
    black: string;
    white: string;
    gray200: string;
    gray400: string;
    gray600: string;
    gray800: string;
    gray900: string;
    red500: string;
    purple800: string;
};
export declare const rgba: {
    "black0.1": string;
    "black0.2": string;
    "black0.3": string;
    "black0.4": string;
    "black0.5": string;
    "black0.6": string;
    "black0.7": string;
    "black0.8": string;
    "black0.9": string;
    "white0.1": string;
    "white0.2": string;
    "white0.3": string;
    "white0.4": string;
    "white0.5": string;
    "white0.6": string;
    "white0.7": string;
    "white0.8": string;
    "white0.9": string;
};
export declare const gradient: {
    purple: string;
    bluePurple: string;
    green: string;
    red: string;
    pink: string;
};
export declare const theme: {
    bg: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
    tr: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
    t: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
    g: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
    gr: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
    ta: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
    sb: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
    sm: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
    c: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
    tag: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
};
export declare const themeShadow: {
    bg: string;
    tr: string;
    t: string;
    g: string;
    gr: string;
    ta: string;
    sb: string;
    sm: string;
    c: string;
    tag: string;
};
export declare const themeShadowHover: {
    bg_hover: string;
    tr_hover: string;
    t_hover: string;
    g_hover: string;
    gr_hover: string;
    ta_hover: string;
    sb_hover: string;
    sm_hover: string;
    c_hover: string;
    tag_hover: string;
};
